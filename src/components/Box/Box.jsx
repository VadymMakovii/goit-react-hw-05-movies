import styled from 'styled-components';
import {
  space,
  layout,
  color,
  flexbox,
  grid,
  typography,
  shadow,
} from 'styled-system';

export const Box = styled.div`
  ${space}
  ${layout}
  ${color}
  ${flexbox}
  ${grid}
  ${typography}
  ${shadow}
`;
